/** Prettier — single source of truth for code formatting.
 * Run `npm run format` to write, `npm run format:check` in CI/gates.
 * @see https://prettier.io/docs/options
 */
module.exports = {
  printWidth: 100,
  tabWidth: 2,
  useTabs: false,
  semi: true,
  singleQuote: true,
  quoteProps: 'as-needed',
  trailingComma: 'all',
  bracketSpacing: true,
  bracketSameLine: false,
  arrowParens: 'always',
  endOfLine: 'lf',
};
